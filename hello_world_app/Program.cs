using System;
using ImGuiNET;

/*
class Program
{
    static void Main()
    {
        Console.WriteLine("Initializing ImGui.NET...");

        // Initialize ImGui.NET
        ImGui.CreateContext();
        var io = ImGui.GetIO();
        io.Fonts.AddFontDefault();
        
        Console.WriteLine("ImGui.NET initialized. Running ImGui demo...");

        // Main application loop
        while (true)
        {
            // Start a new ImGui frame
            ImGui.NewFrame();

            // Sample ImGui code: create a simple window
            ImGui.Begin("Hello, ImGui!");
            ImGui.Text("This is a simple ImGui window.");
            ImGui.End();

            // End the ImGui frame
            ImGui.Render();

            // You would typically handle input and render ImGui here

            // For simplicity, we'll just break the loop after one frame
            break;
        }

        // Shutdown ImGui.NET
        ImGui.DestroyContext();

        Console.WriteLine("ImGui demo complete.");
    }
}

*/

class Program
{
    static void Main()
    {



        Console.WriteLine("Initializing ImGui.NET...");
        Console.WriteLine("ImGui.NET initialized. Running ImGui demo...");
        Console.WriteLine("ImGui demo complete.");
    }
}